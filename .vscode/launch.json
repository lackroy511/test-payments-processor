{
    "configurations": [
        {
            "name": "Run api",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/payments_api/main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
